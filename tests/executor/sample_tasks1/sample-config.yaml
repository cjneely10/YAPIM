---  # document start

###########################################
## Pipeline input section
INPUT:
  base:  # List pipelines containing input data
    root

###########################################

SLURM:
  ## Set to True if using SLURM
  USE_CLUSTER: false
  ## Pass any flags you wish below
  ## DO NOT PASS the following:
  ## --nodes, --ntasks, --mem, --cpus-per-task
  --qos: unlim
  --job-name: EukMS
  user-id: uid

write:
  workers: 20
  threads: 4  # Each worker needs at least 4 threads to complete this step
  memory: 40G
  time: "4:00:00"

update:
  workers: 20
  threads: 4  # Each worker needs at least 4 threads to complete this step
  memory: 40G
  time: "4:00:00"
  dependencies:
    sed:
      program: sed

...  # document end